{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            // "command": "g++ -g main.cpp -o main.out -lrdkafka++",
            "command": "cmake . && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}